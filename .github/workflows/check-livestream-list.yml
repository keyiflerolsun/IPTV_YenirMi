name: Check Live Stream List

on: 
  push:
    branches:
      - master

  schedule: 
    - cron: '0 0 * * *' # every hour
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check URLs
        uses: paramt/url-checker@master
        with:
          files: "Kanallar/KekikAkademi.m3u"